import type { Config } from 'prettier'

const config: Config = {
  arrowParens: 'avoid',
  bracketSpacing: true,
  endOfLine: 'lf',
  jsxSingleQuote: true,
  semi: false,
  singleQuote: true,
  useTabs: false,
}

export default config
module.exports = config
